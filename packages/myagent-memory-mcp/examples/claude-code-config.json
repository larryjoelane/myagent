{
  "_comment": "Claude Code MCP config. Run `claude mcp add memory -- npx -y myagent-memory-mcp` from your terminal, OR drop this snippet into ~/.claude.json under mcpServers (or your project's .mcp.json).",
  "mcpServers": {
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "myagent-memory-mcp"]
    }
  }
}
