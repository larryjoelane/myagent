{
  "_comment": "Claude Desktop MCP config. Add this to your claude_desktop_config.json under mcpServers. On macOS the file lives at ~/Library/Application Support/Claude/claude_desktop_config.json; on Windows at %APPDATA%\\Claude\\claude_desktop_config.json.",
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "myagent-memory-mcp"],
      "env": {
        "MYAGENT_MEMORY_DIR": "${HOME}/.myagent-memory"
      }
    }
  }
}
